# Tested reproduction environment for Python 3.10 and 3.11.
#
# Install the project with this file as a constraint rather than installing it
# directly, for example:
#   pip install -c requirements/constraints.txt -e '.[models,dev,lucene]'
h5py==3.13.0
numpy==1.26.4
PyYAML==6.0.2
scipy==1.13.1

sentence-transformers==2.7.0
torch==2.2.0
transformers==4.51.0

pyserini==0.24.0

pytest==9.1.1
ruff==0.16.5
